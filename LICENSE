---

## 2️⃣ `pyproject.toml`

```toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "soda-arc-core"
version = "0.1.0"
description = "Primitive-first ARC reasoning core using compositional search"
authors = [{ name = "Doanh1102" }]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "pytest"
]

[tool.pytest.ini_options]
testpaths = ["soda/tests"]